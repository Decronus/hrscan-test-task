import LoginForm from "../components/login-form/LoginForm";

const Main = () => {
    return (
        <div className="main-page">
            <LoginForm />
        </div>
    );
};

export default Main;
